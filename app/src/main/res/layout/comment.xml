<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    android:background="#eff0f2"
    android:padding="8dp">

    <TextView
        android:id="@+id/txtUserEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:text="Walberth Gutierrez Telles"
        android:textSize="15sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/txtUserComment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/txtUserEmail"
        android:maxLines="5"
        android:text="here is the full length comment made by the user" />

    <ProgressBar
        android:id="@+id/progressBarComment"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/txtUserComment"
        android:layout_alignParentStart="true"
        android:visibility="gone" />
</RelativeLayout>